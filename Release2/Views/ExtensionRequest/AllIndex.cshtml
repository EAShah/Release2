@model IEnumerable<Release2.ViewModels.ExtensionRequestViewModel>

@{
    ViewBag.Title = "AllIndex";
}

<h2>AllIndex</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExtendedPC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LMSubmits)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExtRequestStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExtRequestSubmissionDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExtRequestSubmissionDate)
        </th>


        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ExtendedPC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LMSubmits)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtRequestStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtRequestSubmissionDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExtRequestSubmissionDate)
            </td>


            <td>
                @Html.ActionLink("Audit", "Audit", new { id = item.Id }, new { @class = "btn btn-primary" }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-info" }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>

@*<div class="container">
        <button type="button" value="PendingIndex" class="btn btn-primary">Pending Requests</button>
    </div>

    <div class="container">
        <button type="button" value="ApprovedIndex" class="btn btn-success">Approved Requests</button>
    </div>

    <div class="container">
        <button type="button" value="ApprovedIndex" class="btn btn-success">Approved Requests</button>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="button" value="RejectedIndex" class="btn btn-warning" />
        </div>
    </div>*@
<p>
    @Html.ActionLink("Pending Requests", "PendingIndex")
</p>

<p>
    @Html.ActionLink("Approved Requests", "ApprovedIndex")
</p>

<p>
    @Html.ActionLink("Rejected Requests", "RejectedIndex")
</p> 